# Do not run Docker Compose directly with this file. Use:
#   ./scripts/prepare-env.sh local-demo
#   ./scripts/compose.sh local-demo up -d
#
# Environment-specific non-secret templates live in deploy/environments/.
# Generated secrets live under runtime/<environment>/secrets/ and are ignored.
